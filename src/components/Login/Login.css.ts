import { style } from '@vanilla-extract/css';

export const login = style({
  width: 300,
  position: 'relative',
  top: '35%',
  justifyItems: 'center',
  alignSelf: 'center',
})

export const spotifyLogo = style({
  maxWidth: 125,
  padding: "0 0 0 .5em"
})